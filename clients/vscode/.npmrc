# @vscode/wasm-wasi-lsp (0.1.0-pre.x) pins an exact vscode-languageclient
# prerelease as its peer, but only uses stable APIs (Emitter, the stream
# reader/writer) that the stable 10.x also provides. Accept stable.
legacy-peer-deps=true
