[package]
name = "epher-lsp"
version = "0.5.50"
edition = "2021"
license = "MIT"
description = "The epher language server: one synchronous binary every IDE extension shares (ADR-0066)."

[dependencies]
epher-core = { path = "../core" }
epher-shell = { path = "../shell" }
epher-i18n = { path = "../i18n" }
lsp-server = "0.7"
lsp-types = "0.97"
serde_json = "1.0"
crossbeam-channel = "0.5"
