[package]
name = "epher-guide"
version = "0.4.20"
edition = "2021"
description = "The epher user guide: site/guide/*.md embedded at build time, with per-frontend renderers (HTML for the web app, plain text for the TUI)."
license = "MIT"
