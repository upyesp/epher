/* ============================================================================
range-of-data.epher -- the spread from smallest to largest value

The range of the ten exam scores in `d`: the builtin range (guide 1.20),
cross-checked as max(d) - min(d), with the two endpoints shown. The
range is the crudest spread measure: it uses only the two extremes.

Run it from a terminal:
  Linux (deb, rpm):  epher /usr/lib/epher/scripts/statistics/summary/range-of-data.epher
  Windows:           epher "$env:LOCALAPPDATA\epher\scripts\statistics\summary\range-of-data.epher"
  macOS:             epher /Applications/epher.app/Contents/Resources/scripts/statistics/summary/range-of-data.epher
In the REPL:             load one of the paths above
Web/desktop:             paste the whole file into the entry, press Enter
Everything also works line by line in the TUI.

The one knob to edit is `d` below. Everything above the results section
is machinery you can leave alone.
============================================================================ */

// ---- the knob -----------------------------------------------------------------
const d = {72, 85, 91, 64, 88, 79, 83, 95, 70, 86}   // exam scores, 10 students

// ---- the range and its definition -----------------------------------------------
range(d)                    // builtin range: largest minus smallest
max(d) - min(d)             // the textbook formula, max(d) - min(d), agrees
min(d)                      // the smallest score
max(d)                      // the largest score

/* ---- expected output ----
= {72, 85, 91, 64, 88, 79, 83, 95, 70, 86}
= 31
= 31
= 64
= 95
*/