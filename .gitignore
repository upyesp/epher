# Rust build artifacts
/target
**/target

# Trunk / web build output
/dist
/web-dist

# Tauri generated
**/gen/schemas

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
/crates/web/dist
site/guide/*/index.html
node_modules/
# VS Code pilot build output
clients/vscode/out/
clients/vscode/dist/
clients/vscode/server/
*.vsix

# NSIS harness output (compile check)
crates/tauri-app/src-tauri/nsis-check.exe

# NSIS compile-check outputs (makensis writes them next to the harnesses)
**/nsis-check.exe
**/nsis-theme-check.exe
site/scripts-data.json

# temporary local copy for the Pi Web file manager (web-vscode test)
/epher-vscode.vsix
# the staging test build rides the preview site until the test passes
!site/tmp-epher-vscode.vsix
