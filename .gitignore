# Rust build artifacts
/target
**/target

# Trunk / web build output
/dist
/web-dist
# The guide markdown the guide crate's build script copies here for
# trunk to serve; site/guide is the source of truth (ADR-0053)
/crates/web/public/guide
/crates/web/public/guide

# Tauri generated
**/gen/schemas

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
/crates/web/dist
site/guide/*/index.html
node_modules/
# NSIS harness output (compile check)
crates/tauri-app/src-tauri/nsis-check.exe

# NSIS compile-check outputs (makensis writes them next to the harnesses)
**/nsis-check.exe
**/nsis-theme-check.exe
